# Contributing

1. Create a focused branch from `main`.
2. Run `npm install` and make changes test-first.
3. Run `npm run check` and `npm run test:e2e` before opening a pull request.
4. Describe user-visible behavior and include screenshots for visual changes.
